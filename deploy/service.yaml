apiVersion: v1
kind: Service
metadata:
  name: market-scanner
  namespace: factory
  labels:
    app: market-scanner
spec:
  type: NodePort
  selector:
    app: market-scanner
  ports:
    - port: 8090
      targetPort: http
      nodePort: 30090
      protocol: TCP
      name: http
